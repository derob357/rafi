# Rafi Assistant - Environment Variables
# Fill these in and rename to .env

# Webhook URL (from cloudflared tunnel)
WEBHOOK_BASE_URL=https://your-tunnel-url.trycloudflare.com

# Configuration Path
RAFI_CONFIG_PATH=./config.yaml

# Security
OAUTH_ENCRYPTION_KEY=                          # Generate with: openssl rand -base64 32

# MCP remote access (optional — if unset, auth is disabled)
MCP_AUTH_TOKEN=                                # Generate with: openssl rand -base64 32

# Logging
LOG_LEVEL=INFO
